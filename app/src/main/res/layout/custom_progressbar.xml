<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyle"
        android:layout_width="@dimen/_80sdp"
        android:layout_height="@dimen/_80sdp"
        android:indeterminateDrawable="@drawable/gradient_progress"
        app:layout_constraintBottom_toBottomOf="@+id/txtLoading"
        app:layout_constraintEnd_toEndOf="@+id/txtLoading"
        app:layout_constraintStart_toStartOf="@+id/txtLoading"
        android:indeterminateDuration="@android:integer/config_shortAnimTime"
        app:layout_constraintTop_toTopOf="@+id/txtLoading" />

    <TextView
        android:layout_width="@dimen/_50sdp"
        android:layout_height="@dimen/_50sdp"
        android:text="@string/loading"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="visible"
        android:gravity="center"
        android:textStyle="bold"
        android:id="@+id/txtLoading"/>
</androidx.constraintlayout.widget.ConstraintLayout>